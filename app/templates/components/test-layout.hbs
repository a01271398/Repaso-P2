<div class="card w-50">
  <div class="card-body">
    <h4 class="card-title">{{test.nombre}}</h4>
    <p>{{test.fecha}}</p>
    {{#link-to 'test-detail' test.id class="btn btn-primary"}}Ver{{/link-to}}
    <a href="" class="btn btn-primary" {{action 'deleteTest'}} >Borrar</a>
  </div>
</div>

